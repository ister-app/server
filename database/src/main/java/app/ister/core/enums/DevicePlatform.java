package app.ister.core.enums;

/** The platform a registered client device runs on, as reported by the client. */
public enum DevicePlatform {
    ANDROID,
    ANDROID_TV,
    IOS,
    LINUX,
    MACOS,
    WINDOWS,
    WEB,
    OTHER
}
