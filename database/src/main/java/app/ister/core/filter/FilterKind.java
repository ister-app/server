package app.ister.core.filter;

/** The media type a custom filter targets; mirrors the browse kinds a library grid can show. */
public enum FilterKind {
    ARTIST,
    ALBUM,
    TRACK,
    MOVIE,
    SHOW,
    EPISODE
}
