package app.ister.disk.scanner.enums;

public enum DirType {
    NONE,
    MOVIE,
    SHOW,
    SEASON,
    EPISODE,
    ARTIST,
    ALBUM,
    /** A comic series directory (COMIC libraries are series-first). */
    SERIES
}
